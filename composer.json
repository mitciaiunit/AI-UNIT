{
    "name": "aiunit/ai-unit-website",
    "description": "AI Unit Government Website — vanilla PHP application. Composer is used only as a dependency manager (PHPMailer for outbound SMTP mail, phpdotenv for .env loading); there is no framework here.",
    "type": "project",
    "license": "proprietary",
    "require": {
        "php": ">=8.1",
        "phpmailer/phpmailer": "^6.9",
        "vlucas/phpdotenv": "^5.6"
    },
    "config": {
        "optimize-autoloader": true,
        "sort-packages": true
    }
}
